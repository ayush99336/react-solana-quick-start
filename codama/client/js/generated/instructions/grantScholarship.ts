/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { RX_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const GRANT_SCHOLARSHIP_DISCRIMINATOR = new Uint8Array([
  101, 18, 45, 203, 83, 252, 196, 10,
]);

export function getGrantScholarshipDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GRANT_SCHOLARSHIP_DISCRIMINATOR
  );
}

export type GrantScholarshipInstruction<
  TProgram extends string = typeof RX_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountTier extends string | AccountMeta<string> = string,
  TAccountPass extends string | AccountMeta<string> = string,
  TAccountBeneficiary extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? ReadonlyAccount<TAccountCreator>
        : TAccountCreator,
      TAccountTier extends string
        ? WritableAccount<TAccountTier>
        : TAccountTier,
      TAccountPass extends string
        ? WritableAccount<TAccountPass>
        : TAccountPass,
      TAccountBeneficiary extends string
        ? ReadonlyAccount<TAccountBeneficiary>
        : TAccountBeneficiary,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type GrantScholarshipInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GrantScholarshipInstructionDataArgs = {};

export function getGrantScholarshipInstructionDataEncoder(): FixedSizeEncoder<GrantScholarshipInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GRANT_SCHOLARSHIP_DISCRIMINATOR })
  );
}

export function getGrantScholarshipInstructionDataDecoder(): FixedSizeDecoder<GrantScholarshipInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGrantScholarshipInstructionDataCodec(): FixedSizeCodec<
  GrantScholarshipInstructionDataArgs,
  GrantScholarshipInstructionData
> {
  return combineCodec(
    getGrantScholarshipInstructionDataEncoder(),
    getGrantScholarshipInstructionDataDecoder()
  );
}

export type GrantScholarshipAsyncInput<
  TAccountCreator extends string = string,
  TAccountTier extends string = string,
  TAccountPass extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator?: Address<TAccountCreator>;
  tier: Address<TAccountTier>;
  pass?: Address<TAccountPass>;
  /** Beneficiary does not need to sign to receive a scholarship. */
  beneficiary: Address<TAccountBeneficiary>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getGrantScholarshipInstructionAsync<
  TAccountCreator extends string,
  TAccountTier extends string,
  TAccountPass extends string,
  TAccountBeneficiary extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RX_PROGRAM_ADDRESS,
>(
  input: GrantScholarshipAsyncInput<
    TAccountCreator,
    TAccountTier,
    TAccountPass,
    TAccountBeneficiary,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  GrantScholarshipInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountTier,
    TAccountPass,
    TAccountBeneficiary,
    TAccountOwner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    tier: { value: input.tier ?? null, isWritable: true },
    pass: { value: input.pass ?? null, isWritable: true },
    beneficiary: { value: input.beneficiary ?? null, isWritable: false },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.creator.value) {
    accounts.creator.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([99, 114, 101, 97, 116, 111, 114])
        ),
        getAddressEncoder().encode(expectAddress(accounts.owner.value)),
      ],
    });
  }
  if (!accounts.pass.value) {
    accounts.pass.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 97, 115, 115])),
        getAddressEncoder().encode(expectAddress(accounts.tier.value)),
        getAddressEncoder().encode(expectAddress(accounts.beneficiary.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.tier),
      getAccountMeta(accounts.pass),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getGrantScholarshipInstructionDataEncoder().encode({}),
  } as GrantScholarshipInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountTier,
    TAccountPass,
    TAccountBeneficiary,
    TAccountOwner,
    TAccountSystemProgram
  >;

  return instruction;
}

export type GrantScholarshipInput<
  TAccountCreator extends string = string,
  TAccountTier extends string = string,
  TAccountPass extends string = string,
  TAccountBeneficiary extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: Address<TAccountCreator>;
  tier: Address<TAccountTier>;
  pass: Address<TAccountPass>;
  /** Beneficiary does not need to sign to receive a scholarship. */
  beneficiary: Address<TAccountBeneficiary>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getGrantScholarshipInstruction<
  TAccountCreator extends string,
  TAccountTier extends string,
  TAccountPass extends string,
  TAccountBeneficiary extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RX_PROGRAM_ADDRESS,
>(
  input: GrantScholarshipInput<
    TAccountCreator,
    TAccountTier,
    TAccountPass,
    TAccountBeneficiary,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): GrantScholarshipInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountTier,
  TAccountPass,
  TAccountBeneficiary,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    tier: { value: input.tier ?? null, isWritable: true },
    pass: { value: input.pass ?? null, isWritable: true },
    beneficiary: { value: input.beneficiary ?? null, isWritable: false },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.tier),
      getAccountMeta(accounts.pass),
      getAccountMeta(accounts.beneficiary),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getGrantScholarshipInstructionDataEncoder().encode({}),
  } as GrantScholarshipInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountTier,
    TAccountPass,
    TAccountBeneficiary,
    TAccountOwner,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedGrantScholarshipInstruction<
  TProgram extends string = typeof RX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    tier: TAccountMetas[1];
    pass: TAccountMetas[2];
    /** Beneficiary does not need to sign to receive a scholarship. */
    beneficiary: TAccountMetas[3];
    owner: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: GrantScholarshipInstructionData;
};

export function parseGrantScholarshipInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGrantScholarshipInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      tier: getNextAccount(),
      pass: getNextAccount(),
      beneficiary: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getGrantScholarshipInstructionDataDecoder().decode(instruction.data),
  };
}
